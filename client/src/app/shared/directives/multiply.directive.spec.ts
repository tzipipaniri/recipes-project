import { MultiplyDirective } from './multiply.directive';

describe('MultiplyDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiplyDirective();
    expect(directive).toBeTruthy();
  });
});
